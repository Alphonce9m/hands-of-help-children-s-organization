// Only export components that exist in the project
export { default as ImpactSection } from './ImpactSection';

// Types can still be exported if needed elsewhere
export type {
  ImpactStat,
  ImpactStatsProps
} from '@/types/sections';